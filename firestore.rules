rules_version = '2';

// 브라우저는 Firestore에 직접 연결하지 않는다.
// 모든 데이터 요청은 학교 IP를 확인하는 Vercel 서버 API가 처리한다.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
